{
    "uz_meta": {
        "parent": "proxy-service-item-parent"
    },
    "params":{
        "description": "Messages in execution of the proxy service ${.*}$ (currently-polling)",
        "key_": "jmx[org.adroitlogic.ultraesb.detail:Type=ProxyServices,Name=${.*}$][Details.pollingCount]"
    }
}
